build:
    docker:
        web:
            dockerfile: containers/projectify-backend.Dockerfile
            target: projectify-backend
        worker:
            dockerfile: containers/projectify-backend.Dockerfile
            target: projectify-celery
        manage:
            dockerfile: containers/projectify-backend.Dockerfile
            target: projectify-manage
release:
    image: projectify-manage
    command:
        - migrate --noinput
