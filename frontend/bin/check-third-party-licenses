#!/usr/bin/env sh
file=third-party-licenses.txt

if ! npm run fix:third-party-licenses
then
    echo "Could not run npm run fix:third-party-licenses"
    exit 1
fi

if ! git diff --quiet "$file"
then
    echo "Please update $file"
    exit 1
fi
