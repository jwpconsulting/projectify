{
  "name": "tools",
  "version": "1.0.0",
  "description": "# Install",
  "main": "index.js",
  "scripts": {
    "check:render-schema": "ajv -s render.yaml.json -d ../render.yaml"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "syncpack": "^12.3.2"
  },
  "dependencies": {
    "ajv-cli": "^5.0.0",
    "ajv": "^8.17.1"
  }
}
